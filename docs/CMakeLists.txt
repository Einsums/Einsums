#----------------------------------------------------------------------------------------------
# Copyright (c) The Einsums Developers. All rights reserved.
# Licensed under the MIT License. See LICENSE.txt in the project root for license information.
#----------------------------------------------------------------------------------------------

# used while generating sphinx config file
set(doxygen_definition_list
    "DOXYGEN:=1"
    "EINSUMS_EXPORT="
    "EINSUMS_FORCEINLINE="
    "__HIP__="
    "__device_ptr__="
    "__host_ptr__="
    "EINSUMS_HOST="
    "EINSUMS_DEVICE="
    "EINSUMS_HOSTDEV="
)

# EINSUMS_BUILD_PYTHON wires up the per-module apiary codegen commands
# that produce the API-reference JSON the docs are generated from, so it is
# required even though the docs never build/import the compiled extension.
if (NOT EINSUMS_BUILD_PYTHON)
  einsums_error("Building documentation requires Python to be enabled")
endif()

foreach(doxygen_predef ${doxygen_definition_list})
  set(doxygen_definitions "${doxygen_definitions} \"${doxygen_predef}\"")
endforeach()

# Generate rst files with CMake variables and toolchains
set(EINSUMS_CMAKE_OPTIONS_RST "")
foreach(_cat ${EINSUMS_OPTION_CATEGORIES})
  string(REPLACE " " "_" _cat_link "${_cat}")
  set(EINSUMS_CMAKE_OPTIONS_RST
      "${EINSUMS_CMAKE_OPTIONS_RST}* :ref:`${_cat} options <${_cat_link}>`\n"
  )
endforeach()
set(EINSUMS_CMAKE_OPTIONS_RST "${EINSUMS_CMAKE_OPTIONS_RST}\n")

get_cmake_property(_variableNames CACHE_VARIABLES)
foreach(_cat ${EINSUMS_OPTION_CATEGORIES})
  string(REPLACE " " "_" _cat_anchor "${_cat}")
  set(_cat_title "${_cat} options")
  string(REGEX REPLACE "." "-" _cat_title_underline "${_cat_title}")
  set(EINSUMS_CMAKE_OPTIONS_RST
      "${EINSUMS_CMAKE_OPTIONS_RST}.. _${_cat_anchor}:\n\n${_cat_title}\n${_cat_title_underline}\n\n"
  )
  foreach(_variableName ${_variableNames})
    if(${_variableName}Category)
      if(_cat STREQUAL ${_variableName}Category)
        string(REPLACE " " "_" _variableName_anchor "${_variableName}")

        get_property(
          _doc
          CACHE "${_variableName}"
          PROPERTY HELPSTRING
        )
        get_property(
          _type
          CACHE "${_variableName}"
          PROPERTY TYPE
        )
        if(NOT _type STREQUAL "UNINITIALIZED")
          set(_variableName "${_variableName}:${_type}")
        endif()
        set(EINSUMS_CMAKE_OPTIONS_RST "${EINSUMS_CMAKE_OPTIONS_RST}* :option:`${_variableName}`\n")
      endif()
    endif()
  endforeach()
  set(EINSUMS_CMAKE_OPTIONS_RST "${EINSUMS_CMAKE_OPTIONS_RST}\n")

  foreach(_variableName ${_variableNames})
    if(${_variableName}Category)
      if(_cat STREQUAL ${_variableName}Category)
        string(REPLACE " " "_" _variableName_anchor "${_variableName}")

        get_property(
          _doc
          CACHE "${_variableName}"
          PROPERTY HELPSTRING
        )
        get_property(
          _type
          CACHE "${_variableName}"
          PROPERTY TYPE
        )
        if(NOT _type STREQUAL "UNINITIALIZED")
          set(_variableName "${_variableName}:${_type}")
        endif()
        set(EINSUMS_CMAKE_OPTIONS_RST
            "${EINSUMS_CMAKE_OPTIONS_RST}.. option:: ${_variableName}\n\n   ${_doc}\n\n"
        )
      endif()
    endif()
  endforeach()
endforeach()

set(SPHINX_CMAKE_VARIABLES_RST_DEST
    "${CMAKE_CURRENT_BINARY_DIR}/sphinx/generated/CMakeVariables.rst"
)
configure_file(
  "${PROJECT_SOURCE_DIR}/cmake/templates/CMakeVariables.rst.in" ${SPHINX_CMAKE_VARIABLES_RST_DEST}
  @ONLY
)

# Option 2 — modules whose C++ API reference is generated by our libclang
# tool (apiary --emit-cpp-docs-json + render_cpp_rst.py) instead of
# Breathe. Incremental rollout: start with a few, expand once validated.
# Incremental rollout. Wiring a module lowers the GLOBAL warning total even
# when its own pages still show some warnings, because our cpp:* directives
# register globally and resolve cross-page references that Breathe left
# dangling. The heavily-templated modules (Tensor, ComputeGraph, Print) do
# still need the "template-param" rendering work (member-template clauses,
# class template-param name consistency, curated nitpick for param names)
# to make their own pages clean — tracked as the next step.
set(EINSUMS_CPP_DOCS_MODULE_LIST
    Einsums/BLASVendor Einsums/Concepts
    Einsums/StringUtil Einsums/BLAS Einsums/TensorBase Einsums/LinearAlgebra
    Einsums/Tensor Einsums/TensorUtilities Einsums/TensorAlgebra
    Einsums/ComputeGraph Einsums/Runtime Einsums/TypeSupport
    Einsums/Utilities Einsums/Errors Einsums/Logging Einsums/Print
    Einsums/Assertion Einsums/BLASBase Einsums/BlockManager
    Einsums/BufferAllocator Einsums/Comm Einsums/CommandLine
    Einsums/ComputeGraphTypes Einsums/Config Einsums/CXX23
    Einsums/Debugging Einsums/FFT Einsums/GPU Einsums/HPTT
    Einsums/Iterator Einsums/PackedGemm Einsums/Preprocessor
    Einsums/Profile Einsums/Python Einsums/PythonDemo
    Einsums/RuntimeConfiguration Einsums/SIMD Einsums/TaskPool
    Einsums/TensorImpl Einsums/TensorIO Einsums/Version)
    # EinsumsExperimental/{Decomposition,Graph} held back: wiring them tripped
    # a Sphinx cpp-domain crash (AssertionError 'parent_symbol' in
    # _resolve_xref_inner) on the incremental build. Investigate before
    # adding — likely a specific cross-reference with a missing parent scope.
set(EINSUMS_CPP_DOCS_DIR "${CMAKE_CURRENT_BINARY_DIR}/cppdocs")
string(REPLACE ";" " " EINSUMS_CPP_DOCS_MODULES "${EINSUMS_CPP_DOCS_MODULE_LIST}")
if(TARGET apiary)
  set(_cppdocs_stamp "${CMAKE_CURRENT_BINARY_DIR}/.cppdocs.stamp")
  add_custom_command(
    OUTPUT "${_cppdocs_stamp}"
    DEPENDS apiary
            "${PROJECT_SOURCE_DIR}/external/apiary/scripts/gen_cpp_docs.py"
            "${PROJECT_SOURCE_DIR}/external/apiary/scripts/render_cpp_rst.py"
    COMMAND ${Python_EXECUTABLE}
            "${PROJECT_SOURCE_DIR}/external/apiary/scripts/gen_cpp_docs.py"
            --source-dir "${PROJECT_SOURCE_DIR}" --build-dir "${CMAKE_BINARY_DIR}"
            --tool $<TARGET_FILE:apiary> --out-dir "${EINSUMS_CPP_DOCS_DIR}"
            --modules ${EINSUMS_CPP_DOCS_MODULE_LIST}
    COMMAND ${CMAKE_COMMAND} -E touch "${_cppdocs_stamp}"
    COMMENT "Generating C++ API reference (Option 2) for: ${EINSUMS_CPP_DOCS_MODULES}"
    VERBATIM
  )
  list(APPEND sphinx_source_files_build "${_cppdocs_stamp}")
endif()

# Configure Sphinx conf.py
set(EINSUMS_CONF_DOCUMENTATION_SHORT_VERSION "${EINSUMS_VERSION}")
if(EINSUMS_WITH_GIT_TAG)
  set(EINSUMS_CONF_DOCUMENTATION_VERSION "${EINSUMS_WITH_GIT_TAG}")
elseif(EINSUMS_WITH_GIT_BRANCH)
  set(EINSUMS_CONF_DOCUMENTATION_VERSION "${EINSUMS_WITH_GIT_BRANCH}")
else()
  # EINSUMS_VERSION is always available, use as fallback
  set(EINSUMS_CONF_DOCUMENTATION_VERSION "${EINSUMS_VERSION_FULL}")
endif()

set(SPHINX_CONF_PY_DEST "${CMAKE_CURRENT_BINARY_DIR}/sphinx/conf.py")
configure_file("${PROJECT_SOURCE_DIR}/cmake/templates/conf.py.in" ${SPHINX_CONF_PY_DEST} @ONLY)

# Copy the Sphinx source files to build directory.
#
# CONFIGURE_DEPENDS re-globs on every build and re-runs configure when the set
# of source files changes (a file is added or deleted) — without it, a new page
# needs a manual reconfigure and a deleted page is never noticed.
file(
  GLOB_RECURSE sphinx_source_files
  LIST_DIRECTORIES false CONFIGURE_DEPENDS
  RELATIVE "${CMAKE_CURRENT_SOURCE_DIR}/"
  "sphinx/*"
)

# exclude api.rst...
list(FILTER sphinx_source_files EXCLUDE REGEX "sphinx/api/full_api.rst")

foreach(sphinx_source_file ${sphinx_source_files})
  add_custom_command(
    OUTPUT "${CMAKE_CURRENT_BINARY_DIR}/${sphinx_source_file}"
    DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/${sphinx_source_file}"
    COMMAND
      ${CMAKE_COMMAND} -E copy_if_different "${CMAKE_CURRENT_SOURCE_DIR}/${sphinx_source_file}"
      "${CMAKE_CURRENT_BINARY_DIR}/${sphinx_source_file}"
  )
endforeach()

foreach(sphinx_source_file ${sphinx_source_files})
  set(sphinx_source_files_build ${sphinx_source_files_build}
                                "${CMAKE_CURRENT_BINARY_DIR}/${sphinx_source_file}"
  )
  list(APPEND _copied_dest_files "${CMAKE_CURRENT_BINARY_DIR}/${sphinx_source_file}")
endforeach()

# Copy the Sphinx source files of the libs directory to build directory
file(
  GLOB_RECURSE sphinx_libs_source_files
  LIST_DIRECTORIES false CONFIGURE_DEPENDS
  RELATIVE "${PROJECT_SOURCE_DIR}/libs"
  "${PROJECT_SOURCE_DIR}/libs/*/*/docs/*"
)

list(APPEND sphinx_libs_source_files "index.rst" "overview.rst")

foreach(lib IN LISTS EINSUMS_LIBS PYTHON_LIBS)
  file(
    GLOB _sphinx_sources CONFIGURE_DEPENDS
    RELATIVE "${PROJECT_SOURCE_DIR}/libs"
    "${PROJECT_SOURCE_DIR}/libs/${lib}/*.rst"
  )
  list(APPEND sphinx_libs_source_files ${_sphinx_sources})
endforeach()

# Leave EinsumsExperimental undocumented for now: don't copy its overview/
# module pages into the Sphinx tree (its API reference is not generated
# either — see conf.py.in / EINSUMS_CPP_DOCS_MODULE_LIST).
list(FILTER sphinx_libs_source_files EXCLUDE REGEX "EinsumsExperimental")

foreach(sphinx_source_file ${sphinx_libs_source_files})
  add_custom_command(
    OUTPUT "${CMAKE_CURRENT_BINARY_DIR}/sphinx/libs/${sphinx_source_file}"
    DEPENDS "${PROJECT_SOURCE_DIR}/libs/${sphinx_source_file}"
    COMMAND
      ${CMAKE_COMMAND} -E copy_if_different "${PROJECT_SOURCE_DIR}/libs/${sphinx_source_file}"
      "${CMAKE_CURRENT_BINARY_DIR}/sphinx/libs/${sphinx_source_file}"
  )
endforeach()

foreach(sphinx_source_file ${sphinx_libs_source_files})
  set(sphinx_source_files_build ${sphinx_source_files_build}
                                "${CMAKE_CURRENT_BINARY_DIR}/sphinx/libs/${sphinx_source_file}"
  )
  list(APPEND _copied_dest_files
       "${CMAKE_CURRENT_BINARY_DIR}/sphinx/libs/${sphinx_source_file}")
endforeach()

# Harden against stale copies. The copy step above is additive: when a
# hand-written source .rst is DELETED, its previously-copied file lingers in the
# build tree and Sphinx still builds it (this is how a removed page kept
# erroring on incremental builds). ``_copied_dest_files`` is exactly the set we
# copy this configure; compare it to the manifest written last configure and
# remove any orphan. Generated pages (api/, reference/python/, conf.py,
# CMakeVariables, cppdocs) are never in this set, so they are never touched.
# CONFIGURE_DEPENDS above guarantees a deletion re-triggers configure, so this
# prune actually runs when the source set shrinks.
set(_copied_manifest "${CMAKE_CURRENT_BINARY_DIR}/.sphinx_copied.manifest")
if(EXISTS "${_copied_manifest}")
  file(STRINGS "${_copied_manifest}" _prev_copied_dest_files)
  foreach(_old IN LISTS _prev_copied_dest_files)
    if(NOT "${_old}" IN_LIST _copied_dest_files)
      file(REMOVE "${_old}")
    endif()
  endforeach()
endif()
string(REPLACE ";" "\n" _manifest_text "${_copied_dest_files}")
file(WRITE "${_copied_manifest}" "${_manifest_text}\n")

# Generated Python API reference. The PyEinsumsDocs target runs
# ``apiary --emit-docs-json`` per module and renders the result to
# reStructuredText (grouped by submodule) via render_docs_rst.py. Copy the
# rendered pages into the Sphinx reference tree so python_module.rst's
# toctree can include reference/python/index. Additive for now: the
# hand-written einsums.core.* pages remain until their curated content is
# migrated into the C++ doc comments.
if(TARGET PyEinsumsDocs)
  set(_gen_py_docs_src    "${CMAKE_BINARY_DIR}/generated/pybind/docs")
  set(_gen_py_docs_dst    "${CMAKE_CURRENT_BINARY_DIR}/sphinx/reference/python")
  set(_gen_py_docs_stamp  "${CMAKE_CURRENT_BINARY_DIR}/.generated_python_docs.stamp")
  # Depend on the render's *output stamp file* (not the PyEinsumsDocs target):
  # a target dependency only enforces ordering, so the copy would not re-run
  # when the rendered pages change. The file's mtime is what ninja tracks.
  # apiary_aggregate_extension writes the stamp inside DOCS_OUTDIR (which we
  # pass as ${_gen_dir}/docs), so it lives next to the rendered pages — not in
  # the parent generated/pybind dir. Pointing one level too high left the
  # dependency with no producing rule, which a fresh (uncached) build tree
  # surfaced as "missing and no known rule to make it".
  set(_render_stamp       "${_gen_py_docs_src}/.docs.stamp")
  add_custom_command(
    OUTPUT "${_gen_py_docs_stamp}"
    DEPENDS PyEinsumsDocs "${_render_stamp}"
    COMMAND ${CMAKE_COMMAND} -E make_directory "${_gen_py_docs_dst}"
    COMMAND ${CMAKE_COMMAND} -E copy_directory "${_gen_py_docs_src}" "${_gen_py_docs_dst}"
    COMMAND ${CMAKE_COMMAND} -E touch "${_gen_py_docs_stamp}"
    COMMENT "docs: copying generated Python API reference into the Sphinx tree"
  )
  list(APPEND sphinx_source_files_build "${_gen_py_docs_stamp}")
endif()

# Create links to source files so that they are visible to sphinx
# create_symbolic_link("${PROJECT_SOURCE_DIR}/src" "${CMAKE_CURRENT_BINARY_DIR}/src")
# create_symbolic_link("${PROJECT_SOURCE_DIR}/Einsums" "${CMAKE_CURRENT_BINARY_DIR}/Einsums")
create_symbolic_link("${PROJECT_SOURCE_DIR}/examples" "${CMAKE_CURRENT_BINARY_DIR}/examples")
create_symbolic_link("${PROJECT_SOURCE_DIR}/tests" "${CMAKE_CURRENT_BINARY_DIR}/tests")
create_symbolic_link("${PROJECT_SOURCE_DIR}/libs" "${CMAKE_CURRENT_BINARY_DIR}/libs")

add_custom_target(docs)
add_custom_target(docs-html)
add_custom_target(docs-singlehtml)
add_custom_target(docs-latexpdf)
add_custom_target(docs-man)

set(SPHINX_DOCS_HTML_OUTPUT_FILE "index.html")
set(SPHINX_DOCS_SINGLEHTML_OUTPUT_FILE "index.html")
set(SPHINX_DOCS_PDF_OUTPUT_FILE "latex/Einsums.pdf")
set(SPHINX_DOCS_MAN_OUTPUT_FILE "Einsums.1")

foreach(output_format ${EINSUMS_WITH_DOCUMENTATION_OUTPUT_FORMATS})
  string(TOUPPER ${output_format} output_format_upper_case)
  set(SPHINX_DOCS_OUTPUT_DIR "${PROJECT_BINARY_DIR}/share/Einsums/docs/${output_format}")
  set(SPHINX_DOCS_OUTPUT_FILE
      "${SPHINX_DOCS_OUTPUT_DIR}/${SPHINX_DOCS_${output_format_upper_case}_OUTPUT_FILE}"
  )

  # NOTE: The PDF does not build without errors but still produces a reasonable PDF. If there are
  # real errors this will ignore them. Fixing the errors is at the moment not worth the effort.
  if(${output_format} STREQUAL "latexpdf")
    set(SPHINX_DOCS_BUILD_COMMAND
        ${Sphinx_EXECUTABLE} -M ${output_format} "${CMAKE_CURRENT_BINARY_DIR}/sphinx"
        "${SPHINX_DOCS_OUTPUT_DIR}" || (exit 0)
    )
  else()
    # -n  nitpicky (every dangling cross-reference is a warning)
    # -W  treat warnings as errors  --keep-going  report them all, not just the first
    set(SPHINX_DOCS_BUILD_COMMAND
        ${Sphinx_EXECUTABLE} -b ${output_format} -n -W --keep-going -d
        "${CMAKE_CURRENT_BINARY_DIR}/doctree" "${CMAKE_CURRENT_BINARY_DIR}/sphinx"
        "${SPHINX_DOCS_OUTPUT_DIR}"
    )
  endif()

  # The reference is generated statically from headers by apiary, so the
  # docs depend on the codegen stamps in ``sphinx_source_files_build`` (which pull
  # in the apiary tool and PyEinsumsDocs) — NOT on the compiled Einsums
  # library or the PyEinsums (_core) extension. Depending on those would force a
  # full library + extension build for no reason.
  add_custom_command(
    OUTPUT "${SPHINX_DOCS_OUTPUT_FILE}"
    DEPENDS "${sphinx_source_files_build}"
    COMMAND ${SPHINX_DOCS_BUILD_COMMAND}
  )

  add_custom_target(docs-${output_format}-file ALL DEPENDS ${SPHINX_DOCS_OUTPUT_FILE})
  add_dependencies(docs-${output_format} docs-${output_format}-file)

  add_dependencies(docs docs-${output_format})
endforeach()


add_custom_target(
  git_docs
  COMMAND
    "${CMAKE_COMMAND}" -DEINSUMS_BINARY_DIR:PATH=${PROJECT_BINARY_DIR}
    -DEINSUMS_VERSION:STRING=${EINSUMS_VERSION}
    -DEINSUMS_WITH_GIT_BRANCH=${EINSUMS_WITH_GIT_BRANCH}
    -DEINSUMS_WITH_GIT_TAG=${EINSUMS_WITH_GIT_TAG}
    -DEINSUMS_WITH_DOCUMENTATION_OUTPUT_FORMATS="${EINSUMS_WITH_DOCUMENTATION_OUTPUT_FORMATS}" -P
    "${PROJECT_SOURCE_DIR}/cmake/Einsums_UpdateGitDocs.cmake"
)

set_target_properties(git_docs PROPERTIES FOLDER "Documentation/")

add_dependencies(git_docs docs)
