name: einsums-dev
channels:
  - conda-forge
  - nodefaults
# macOS-specific, compiler-agnostic packages go here. The clang toolchain
# (incl. clang-tools + llvm-openmp) is injected by merge_yml.py.
dependencies: []
